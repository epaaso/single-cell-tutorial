SERVER=sefirot.inmegen.gob.mx
PORT=8000

l_TELNET=`echo "quit" | telnet $SERVER $PORT | grep "Escape character is"`
if [ "$?" -ne 0 ]; then
    ssh -p 5265 sefirot.inmegen.gob.mx
else
    ssh sefirot.inmegen.gob.mx
fi